[build]
  publish = "dist"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

[functions."generate-interior"]
  timeout = 120

[functions."bb-menu"]
  timeout = 120

[functions."gemini-brandboss"]
  timeout = 120

[functions."bb-logo"]
  timeout = 60

# gemini-3.6-flash는 thinking 토큰 때문에 응답이 느려 10초 기본값으로는 부족
[functions."gemini-rebrandboss"]
  timeout = 120

[functions."bb-brandname"]
  timeout = 60

[functions."bb-competition"]
  timeout = 60

[functions."bb-save"]
  timeout = 30

[functions."bb-projects"]
  timeout = 30

[dev]
  command = "npm run dev"
  targetPort = 5173
  port = 8888

[[redirects]]
  from = "/share/*"
  to = "/index.html"
  status = 200
